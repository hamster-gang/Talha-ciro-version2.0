// CIRO — widget smoke test
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CIRO smoke test placeholder', (WidgetTester tester) async {
    // Skipped: app requires async EnvConfig init and live backend.
    // Full testing is done via manual end-to-end demo flow.
  });
}
